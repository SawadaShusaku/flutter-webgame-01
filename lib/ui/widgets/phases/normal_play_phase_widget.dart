import 'package:flutter/material.dart';
import 'package:test_web_app/ui/screens/normal_play_screen.dart';

/// 通常プレイフェーズのWidget
/// 既存のNormalPlayScreenをそのまま使用
class NormalPlayPhaseWidget extends StatelessWidget {
  const NormalPlayPhaseWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const NormalPlayScreen();
  }
}
